# k8s/secret.yaml
# TEMPLATE ONLY — use real secrets via Vault / Sealed Secrets / External Secrets
# DO NOT commit real values to source control
apiVersion: v1
kind: Secret
metadata:
  name: fte-secrets
  namespace: fte-system
  annotations:
    # Use external-secrets.io or sealed-secrets in production
    secret.management: "external-secrets"
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_WITH_REAL_KEY"
  DATABASE_URL: "postgresql+asyncpg://fte_user:REPLACE@postgres-svc:5432/customer_fte"
  DATABASE_URL_SYNC: "postgresql://fte_user:REPLACE@postgres-svc:5432/customer_fte"
  TWILIO_ACCOUNT_SID: "REPLACE_WITH_REAL_SID"
  TWILIO_AUTH_TOKEN: "REPLACE_WITH_REAL_TOKEN"
  TWILIO_WHATSAPP_NUMBER: "whatsapp:+14155238886"
  API_SECRET_KEY: "REPLACE_WITH_32_CHAR_RANDOM_STRING"
